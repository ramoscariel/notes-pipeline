Proyecto de prueba CICD, CircleCI, Argo CD, LaunchDarkly.

## Set up

1. Create virtual environment

`python -m venv .venv`

2. Activate venv

`.venv\Scripts\activate`

3. Install dependencies

`pip install -r requirements.txt`


## Run

`uvicorn app.main:app --reload`